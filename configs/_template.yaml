train:
  exp_id: exp_name

  # Model choice + hyper-params
  model_name: edge_mlp  # edge_mlp | edge_mlp_deep | edge_res_mlp | edge_transformer
  hidden: 128
  depth: 2
  dropout: 0.0
  # Transformer-only (optional):
  # n_heads: 8
  # ff_mult: 3
  # edge_feat_mode: relative_sincos_v2  # full | relative | relative_sincos | relative_sincos_v2

  # Optional training overrides
  # Default training flow is simple:
  #   epochs=10, val_every=1, lr_scheduler=plateau
  #   early_stop=true, early_patience=2
  # batch_size: 64
  # lr: 0.001
  # weight_decay: 0.00005
  # seed: 0
  # Only set advanced options if intentionally needed:
  # lr_scheduler: plateau   # none | plateau
  # val_every: 1

  # Optional dataset overrides
  # train_root: runs/data/synthetic/train
  # val_root: runs/data/synthetic/val

# Evaluate on TSPLIB (recommended for leaderboard-style comparison)
eval:
  device: cuda
  model_path: runs/experiments/{exp_id}/latest.json
  # save_pred_tour: false   # keep eval JSON compact
  # Decode options (defaults keep old behavior):
  # decode_multistart: 1
  # decode_noise_std: 0.0
  # decode_twoopt_passes: 20
  data_roots:
    - runs/data/tsplib/processed

# Visualize: all TSPLIB instances
visualize:
  model: runs/experiments/{exp_id}/latest.json
  # Decode options for predict-mode figures:
  # decode_multistart: 1
  # decode_noise_std: 0.0
  # decode_twoopt_passes: 20
  targets:
    - npz_dir: runs/data/tsplib/processed
      limit: 0
  out_dir: auto
