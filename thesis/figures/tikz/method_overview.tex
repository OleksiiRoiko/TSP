\begin{tikzpicture}[
    font=\small,
    box/.style={
        draw, rounded corners=4pt, align=center,
        text width=2.75cm, minimum height=1.15cm,
        inner sep=4pt, fill=white
    },
    widebox/.style={
        draw, rounded corners=4pt, align=center,
        text width=3.05cm, minimum height=1.15cm,
        inner sep=4pt, fill=white
    },
    arrow/.style={-{Latex[length=2.2mm]}, semithick},
    group/.style={
        draw, rounded corners=7pt,
        inner xsep=14pt, inner ysep=10pt,
        fill=gray!7
    }
]

%% ── TRAINING NODES ───────────────────────────────
\node[font=\bfseries]  (Thead)     at (3.0,  0.80) {Tréningová vetva};
\node[box]             (synthetic) at (3.0, -0.55) {Synteticky\\generované\\inštancie};
\node[box]             (heuristic) at (0.7, -3.15) {Približné\\referenčné\\trasy};
\node[box]             (concorde)  at (5.3, -3.15) {Presné\\referenčné\\trasy};
\node[box]             (labels)    at (3.0, -5.55) {Tvorba cieľových\\štítkov};
\node[box]             (train)     at (3.0, -7.65) {Tréning\\a validácia\\modelov};

%% ── SHARED / MIDDLE BLOCK ────────────────────────
\node[widebox]         (predict)   at (7.8, -7.65) {Predikcia hrán,\\dekódovanie\\a lokálne\\zlepšenie};

%% ── BENCHMARK NODES ──────────────────────────────
\node[font=\bfseries]  (Bhead)     at (12.5,  0.80) {Vetva TSPLIB};
\node[box]             (tsplib)    at (12.5, -0.55) {Referenčné\\inštancie\\TSPLIB};
\node[widebox]         (eval)      at (12.5, -5.15) {Hodnotenie na\\inštanciách\\TSPLIB};

%% ── BOTTOM BLOCK ─────────────────────────────────
\node[widebox]         (analysis)  at (10.4,-10.55) {Analýza výsledkov\\a interpretácia};

%% ── GROUP FRAMES (background, fit over header+nodes) ──
\begin{scope}[on background layer]
    \node[group, fit=(Thead)(synthetic)(heuristic)(concorde)(labels)(train)] {};
    \node[group, fit=(Bhead)(tsplib)(eval)] {};
\end{scope}

%% ── ARROWS ───────────────────────────────────────
% Synthetic → Heuristic / Concorde
\draw[arrow] (synthetic.south) -- ++(0,-0.35) -| (heuristic.north);
\draw[arrow] (synthetic.south) -- ++(0,-0.35) -| (concorde.north);

% Heuristic / Concorde → Labels
\draw[arrow] (heuristic.south) |- (labels.west);
\draw[arrow] (concorde.south)  |- (labels.east);

% Labels → Train → Predict
\draw[arrow] (labels.south) -- (train.north);
\draw[arrow] (train.east)   -- (predict.west);

% TSPLIB → Eval
\draw[arrow] (tsplib.south) -- (eval.north);

% Predict → Eval
\coordinate (pred_eval_turn) at ($(predict.east)+(0.75,0)$);
\coordinate (eval_entry) at ([xshift=-0.35cm]eval.west);
\draw[arrow] (predict.east) -- (pred_eval_turn) |- (eval_entry) -- (eval.west);

% Predict / Eval → Analysis
\draw[arrow] (predict.south) -- ++(0,-0.55) |- (analysis.west);
\draw[arrow] (eval.south)    -- ++(0,-0.65) |- (analysis.east);

\end{tikzpicture}
